
/**
 * Transaction interface. It forces a transaction to process itself.
 * Every transaction class implements this interface.
 * @author Mert
 *
 */
public interface Transaction {
	public void processTransaction();
}
